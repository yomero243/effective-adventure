export const focusCameraOnPoint = (section) => {
  // Lógica para enfocar la cámara en el punto correspondiente
  console.log(`Focusing camera on section: ${section}`);
  // Aquí puedes agregar la lógica específica para mover la cámara
};